import { defineConfig } from 'vite'
// ... autres imports

export default defineConfig({
  // Remplace 'nom-de-ton-repo' par le nom exact de ton dépôt sur GitHub
  base: '/SA-3.Crea.03--main/', 
  plugins: [
    // ... tes plugins
  ],
})